alert("Vamos iniciar")


const base = Number (prompt(" Digite a base do seu retângulo: "))
const altura = Number (prompt("Agora digite a altura do retângulo: "))

const area = base * altura
alert("A área do seu retângulo é:  " + area)

const perimetro = (base * 2) + (altura * 2)
alert ("O perímetro do retângulo é: " + perimetro)