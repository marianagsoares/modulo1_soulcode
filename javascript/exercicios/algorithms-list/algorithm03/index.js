alert("Vamos iniciar")

const raio = Number (prompt(" Digite o raio da circunferência: "))
const pi = 3.14

const areacircunferencia = pi * Math.pow(raio, 2)
alert(" A área da circunferência é: " + areacircunferencia)

const perimetrocircunferencia = 2 * pi * raio
alert(" O perímetro da circunferência é: " + perimetrocircunferencia)